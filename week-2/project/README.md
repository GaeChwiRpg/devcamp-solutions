# Week 2 Project — Week 1 코드를 JPA로 리팩토링

이 폴더에는 JPA 전환 예시를 실제 코드로 넣었습니다.

핵심 포인트:

- `domain/post`, `domain/comment`: 엔티티와 연관관계
- `repository/`: JPA 리포지토리와 fetch join 조회
- `service/`: 화면 응답으로 묶기 전 조회 흐름

학생은 이 폴더를 보고
"Week 1 코드를 JPA로 바꿀 때 어디까지 제출하면 되는지"
감만 잡으면 됩니다.
