# Week 1 Project — 본인이 처음부터 만드는 첫 Spring Boot 프로젝트

이 폴더에는 실제로 제출하는 최소 Spring Boot 코드 예시를 넣었습니다.

핵심 포인트:

- `api/post/`: HTTP 입출력
- `service/`: 게시글 생성/조회/수정 흐름
- `repository/`: 메모리 저장소 예시
- `test/`: 최소 서비스 테스트

학생은 이 폴더를 보고
"이 정도 크기의 실제 코드가 PR에 같이 올라가는구나"
를 이해하면 됩니다.
